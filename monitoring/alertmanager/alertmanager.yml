global:
  resolve_timeout: 1m

route:
  receiver: default-receiver
  group_by: ['alertname', 'job']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: default-receiver